services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: photoapp
      POSTGRES_PASSWORD: photoapp
      POSTGRES_DB: photoapp
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
